{
    "configurations": [
        {
            "name": "darkfeed [:darkfeed:run]",
            "type": "gradle",
            "workingDir": "$PROJECT_DIR$",
            "tasks": [
                ":darkfeed:run"
            ],
            "environmentFile": "local.env",
            "initScripts": {
                "flmapper": "ext.mapPath = { path -> path }"
            }
        }
    ]
}